// Reusable number pad (Login uses 0-9 + A-D; Payment uses 0-9 only).
// See design-docs/03-ui-components.md (Shared Components).

export function NumberPad() {
  // TODO: configurable key set + onKey(value)
  return null;
}
