// In-memory mock store + mutation helpers (seeded once at startup).
// See design-docs/04-api-contract.md (MSW Implementation Notes).

// TODO: assemble the in-memory DB from ./seed and expose query/mutation helpers.
