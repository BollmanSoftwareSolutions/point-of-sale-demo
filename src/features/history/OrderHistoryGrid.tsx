// Results grid — Order #, Date, Status, Items (10 rows, paginated).
// See design-docs/03-ui-components.md §3.

export function OrderHistoryGrid() {
  // TODO: MUI DataGrid, newest -> oldest, row click -> detail
  return null;
}
